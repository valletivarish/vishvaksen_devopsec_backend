package com.mealplanner.api.model.enums;

/**
 * Represents the type of meal within a daily meal plan.
 * Used by MealPlanEntry to categorize when a recipe is consumed.
 */
public enum MealType {
    BREAKFAST,
    LUNCH,
    DINNER,
    SNACK
}
